apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage
spec:
  storageClassName: local-storage
  volumeName: storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Gi
